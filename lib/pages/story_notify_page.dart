import 'package:flutter/material.dart';

class StoryNotifyPage extends StatelessWidget {
  const StoryNotifyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Notify"),
      ),
    );
  }
}
